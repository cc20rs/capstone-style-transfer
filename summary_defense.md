# 小样本叙事风格迁移 MVP｜面试/答辩展示版

## 0. 一句话项目定位
在**尽量不改事实**的前提下，把叙事文本迁移为指定文风，并用“主观+客观+语言学+流利度”四类指标做自动评测；本轮已扩展到 **5 个 case、15 条结果**，并固定评估方法以保证多次实验可比。

---

## 1. 业务问题与目标（你在解决什么）
- 输入：事实文本 + 目标风格标签（如鲁迅、钱锺书）
- 输出：风格化改写文本
- 核心挑战：
  - 既要“像目标风格”
  - 又要“别胡编事实”
- 本项目目标：做一个可复现实验流水线，对比三种策略并输出结构化报告

---

## 2. 方案总览（你是怎么做的）
### 2.1 三条基线策略
- **Baseline A（Prompt Zero-shot）**：只给指令，不给风格样例
- **Baseline B（Context Few-shot）**：给固定 2~3 条风格样例
- **Baseline C（RAG + ICL）**：先检索与原文最相关的风格片段，再喂给模型

### 2.2 统一 Pipeline
1) 读取测试集
2) 按三策略生成文本
3) 多指标评估
4) 每条结果实时写入 JSONL（防中断）
5) 导出 Markdown 对比报告，支持答辩展示

---

## 3. 评估指标（直观解释版）
### 3.1 内容保真：`bert_score`（越高越好）
- 含义：生成文本与原始事实语义的一致性
- 解读：高分代表“事实保留得更好”
- 本轮实际方法：**标准 `BERTScore-F1`**，15 条结果全部一致

### 3.2 风格主观：`llm_judge_score`（0~10，越高越好）
- 含义：由 LLM 从词汇、句法、情绪三维打分
- 解读：高分代表“读起来更像目标作者”
- 当前主流程保留主裁判总分，同时落盘双裁判明细用于对比

### 3.3 风格客观：`style_vector_score`（越高越好）
- 含义：生成文本向量与目标风格向量中心的余弦相似度
- 解读：高分代表“向量空间更接近目标风格”

### 3.4 流利度：`fluency_score = 100/NLL`（越高越好）
- 含义：语言模型困惑度的反向分数
- 解读：高分代表“句子更顺、更自然”
- 本轮实际方法：**标准 `100/NLL`**，15 条结果全部一致

### 3.5 语言学迁移轨迹（不做单分）
- `avg_len`（句长）
- `ttr`（词汇丰富度）
- `adj_density`（形容词密度）
- `adv_density`（副词密度）
- 解读：看 generated 是否向 target 靠拢，而不是简单追求“大/小”

---

## 4. 本次实验配置（你真实跑了什么）
### 4.1 在线接口
- 协议：OpenAI Compatible Chat Completions
- 网关：`https://llmapi.paratera.com/v1`
- 生成模型：`DeepSeek-V3.2-Instruct`
- 裁判模型：`DeepSeek-V3.2`、`Qwen3-Next-80B-A3B-Instruct`

### 4.2 评估相关模型
- BERTScore：`bert-base-chinese`
- Style Embedding：`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- Fluency NLL：`uer/gpt2-chinese-cluecorpussmall`

### 4.3 参数
- `temperature=0.7`
- `max_tokens=1024`
- `rag.top_k=3`
- `api.timeout=60s, retries=3`
- HuggingFace 国内镜像：`https://hf-mirror.com`

---

## 5. 结果看板（可直接讲）
### 5.1 样本规模
- **5 个 case × 3 个策略 = 15 条有效结果**
- 风格分布：鲁迅为主，包含钱锺书样本；既有正常叙事，也有人为构造的噪声/夸张样本

### 5.2 策略均值（核心结论）
- **Baseline B（Static Few-shot）在综合客观指标上最稳**
  - `bert_score` 最高（≈0.7512）
  - `llm_judge_score` 次高（≈6.64）
  - `style_vector_score` 最高（≈0.4995）
  - `fluency_score` 最高（≈22.82）
- **Baseline C（RAG+ICL）主观风格感最强**
  - `llm_judge_score` 最高（≈8.08）
  - 但 `bert_score`（≈0.7255）与 `style_vector_score`（≈0.4899）略低于 Baseline B
- **Baseline A（Zero-shot）可作为低成本基线**
  - `bert_score` 接近 B（≈0.7498）
  - 但主观风格分明显偏低（≈5.66）

### 5.3 关键发现
- **Few-shot 仍然是当前最稳妥方案**：在保真、向量相似度、流利度三个维度上整体领先
- **RAG 的优势主要体现在“风格感”**：在 `case_001 / case_003 / case_004 / case_005` 上都拿到了最高主观风格分
- **不同样本最优策略不同**：
  - `case_001`：RAG 在主观风格和向量相似度上都最好
  - `case_002`：Static Few-shot 在主观风格和向量相似度上都最好
  - `case_003`：Static Few-shot 的保真最高，RAG 的主观风格最高
  - `case_004`：Static Few-shot 的保真最高，但 Zero-shot 的向量分意外最高
  - `case_005`：RAG 的保真和主观风格都最好，但 Zero-shot 的向量分更高
- 结论不是“某一个策略全赢”，而是：
  - **要稳定上线，优先 Baseline B**
  - **要追求更强风格感，可继续优化 Baseline C 的检索链路**

---

## 6. 工程亮点（面试加分点）
- 配置化 + 模块化：生成器/评估器统一接口，方便扩展
- 中间态 `case_summary.jsonl` 逐条落盘：实验可追溯、可恢复
- 三策略共享同一主流程：保证对比公平
- 评估体系完整：主观与客观指标互补
- 已把表一核心方法固定为：`BERTScore-F1`、SentenceTransformer 余弦相似度、`100/NLL`、jieba 语言学统计，避免每轮实验方法漂移
- 模型下载链路做了国内化处理：优先 ModelScope，本地无同名仓库时走 HuggingFace 国内镜像

---

## 7. 局限与下一步（体现工程判断）
- 当前样本量仍偏小（n=5），趋势可信度高于统计显著性
- RAG 检索链路仍可优化（召回 + 重排 + 风格约束）
- 当前双裁判结果已落盘，但主流程总分仍以首裁判兼容字段为主；后续可加入双裁判融合总分
- 下一步计划：
  1) 扩到 30~100 case
  2) 引入“事实约束解码/后验校验”降低幻觉
  3) 对 RAG 增加风格纯度过滤与重排
  4) 做分风格分层报告与显著性检验

---

## 8. 3 分钟答辩讲稿（可直接念）
> 我做的是一个小样本叙事风格迁移 MVP，目标是在不改变事实的前提下，把普通叙事文本改写成指定作者风格。  
> 方案上我搭了统一 pipeline，对比了三种策略：Zero-shot、Static Few-shot 和 RAG+ICL。  
> 评估上我没有只看一个指标，而是组合了语义保真、主观风格评分、风格向量相似度、流利度以及语言学特征迁移轨迹。并且我把表一的指标方法固定下来，避免每次实验因为下载失败或回退策略不同而导致结果不可比。  
> 最新一轮实验共 5 个 case、15 条结果。整体来看，Static Few-shot 是当前最稳的策略，在保真、向量相似度和流利度上综合领先；RAG+ICL 的主观风格感最强，但稳定性更依赖检索质量。  
> 工程上，我把整个过程做成了可复现、可追溯的系统：配置化、模块化、中间态 JSONL 即时落盘、Markdown 报告自动导出，并且补上了国内下载镜像支持。下一步我会继续扩样本规模，并强化 RAG 检索与事实约束。  

---

## 9. 30 秒电梯版
我把“风格迁移”做成了可复现实验系统，不只是能生成，还能稳定评估。最新 5 个样本结果表明：**Static Few-shot 是当前最稳基线，RAG 在主观风格上最有潜力，但还需要更好的检索质量来稳定收益。**
